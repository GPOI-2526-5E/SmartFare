import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.smartfare.app',
  appName: 'Smartfare-app',
  webDir: 'dist/SmartFare/browser'
};

export default config;
